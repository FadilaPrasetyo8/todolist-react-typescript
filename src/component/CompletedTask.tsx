import React from "react";
import { IAppProps } from "../interfaces";

interface CompletedProps {
  completedd: IAppProps;
}

const CompletedList: React.FC<CompletedProps> = ({ completedd }: CompletedProps) => {
  return <></>;
};

export default CompletedList;
